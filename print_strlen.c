#include "main.h"
/**
 * _strlen - returns length of a string
 * @str: string input
 * Return: 1 on success
 */
int _strlen(char *str)
{
	int i;

	for (i = 0; str[i] != '\0'; i++)
	{
		
	}
	return (i);
}
/**
 * _strlenc - prints string for constant char
 * @str: string input
 * Return: 1 on success
 */
int _strlenc(const char *str)
{
	int i;

	for (i = 0; str[i] != '\0'; i++)
	{
		
	}
	return (i);
}
